function solarized-dark
    alacritty msg config "$(cat ~/.config/alacritty/solarized_dark.toml)"
end

function solarized-light
    alacritty msg config "$(cat ~/.config/alacritty/solarized_light.toml)"
end
